.ragister-account.text-center.tr-section.tr-section-padding
  .account-content
    .section-title
      %h1= t :notunlock

    = bootstrap_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: 'contact-form' }) do |f|
      = f.email_field :email, autofocus: true, autocomplete: 'email', placeholder: 'Email', hide_label: true
      = f.primary t(:resend)

  .options= render '/users/links'