.animated.fadeIn

  - if photo.year
    %p
      %strong= photo.year

  - if photo.content
    %p.text-muted= photo.content

  - if photo.author?
    %p.author= t 'photos.author', author: photo.author
  - elsif photo.user
    %p.author= t 'photos.author_html', author: (link_to photo.user.login, photo.user)

  - if can? :edit, photo
    = link_to edit_place_photo_path(photo.place, photo), class: 'btn btn-mini btn-secondary' do
      = fa_icon 'pencil-alt'
  - if can? :destroy, @place
    = link_to [photo.place, photo], method: 'delete', data: { confirm: t('actions.sure') }, class: 'btn btn-danger' do
      = fa_icon 'trash'
