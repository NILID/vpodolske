= bootstrap_form_for @letter, :html => { :class => 'contact-form' } do |f|
  = f.text_area :content
  = f.text_field :email
  = f.primary
  = link_to t('actions.cancel'), letters_path, :class => 'btn btn-default'
